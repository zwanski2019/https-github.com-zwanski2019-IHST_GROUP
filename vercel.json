{
  "build": {
    "env": {
      "VITE_API_URL": "https://api.example.com"
    },
    "use": "@vercel/static-build",
    "config": {
      "distDir": "dist"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "framework": "vite"
}
